// invalid for sema: a for-loop's own const-declared init variable being
// mutated in its step clause must still be flagged (Item B1).
fn entry() -> i64 {
    for (const i: i64 = 0; i < 10; i = i + 1) { }
    return 0;
}
