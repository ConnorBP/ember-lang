// invalid for sema: shift by a negative constant count is undefined behavior
// and almost always a bug. Sema must hard-error on a compile-time negative
// shift amount rather than silently masking it (x << -1 currently yields 0).
fn bad(x: i64) -> i64 {
    return x << -1;
}
