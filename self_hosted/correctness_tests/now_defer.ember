// Self-hosted positive: defer at function return (was reject -312).
// The return value is captured before the deferred expr runs, so result=42.
// expect: 42
fn cleanup(x: i64) -> i64 { return x; }
fn main() -> i64 { let mut result: i64 = 0; defer cleanup(0); result = 42; return result; }
