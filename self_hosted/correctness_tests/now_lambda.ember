// Self-hosted positive: infer a lambda's function-handle type and call it.
// The by-value argument is passed through the lambda ABI; 41 + 1 = 42.
// expect: 42
fn main() -> i64 {
    let increment = fn(value: i64) -> i64 { return value + 1; };
    return increment(41);
}
