// Native-valid, self-hosted out-of-subset rejection expected: -302.
// selfhost-reject: -302
enum State { Off = 0, On = 1 }
fn main() -> i64 { let s: State = State::On; if (s == State::On) { return 42; } return 0; }
