// Self-hosted positive: switch basic (converted from reject_switch.ember once
// Phase 3b added switch support). Native returns 42; self-hosted must match.
// expect: 42
fn main() -> i64 { let mut r: i64 = 0; switch (2) { case 2: r = 42; break; default: r = 1; break; } return r; }
