// Self-hosted positive: do-while loop (body runs before the cond is tested).
// Sums 0+1+2+3+4 = 10. break/continue work inside (loop depth incremented).
// expect: 10
fn main() -> i64 { let mut i: i64 = 0; let mut sum: i64 = 0; do { sum = sum + i; i = i + 1; } while (i < 5); return sum; }
