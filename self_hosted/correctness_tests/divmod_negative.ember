// Differential audit: signed division truncates toward zero; remainder follows dividend.
// expect: 67
fn main() -> i64 {
    let a: i64 = -20 / 6;
    let b: i64 = -20 % 6;
    let c: i64 = 20 / -6;
    let d: i64 = 20 % -6;
    return (a + 10) * 10 + (b + c + d);
}
