// Block-partition pair B: extra lexical blocks. Must equal block_partition_a.
// expect: 42
fn main() -> i64 {
    let mut x: i64 = 1;
    { x = x + 2; }
    {
        { x = x * 3; }
        if (x == 9) { { x = x + 33; } }
    }
    return x;
}
